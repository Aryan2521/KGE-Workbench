# Run artifacts

The application creates one directory per run here. Each directory contains the validated configuration, raw execution log, and structured result JSON. Generated run directories are intentionally excluded from version control.
